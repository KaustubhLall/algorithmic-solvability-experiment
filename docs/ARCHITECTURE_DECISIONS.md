# ARCHITECTURE DECISIONS

> **PURPOSE:** Permanent record of every architectural and design decision made during implementation.
> This supplements the Deviation Log in `EXPERIMENT_CATALOG.md` Part 5.
> The Deviation Log records *changes from plan*. This document records *decisions within implementation*
> that aren't deviations but are still worth preserving for future context.
>
> **Last Updated:** 2025-03-25 (TASK-01 complete)
> **Format:** Append only. Never modify or delete past entries.

---

## How to Add an Entry

When you make a non-trivial architectural decision during implementation — e.g., choosing a data structure, picking a library, deciding how to handle an edge case, choosing an abstraction boundary — add an entry here using the template below.

**Threshold for logging:** If you thought about more than one option, log it.

```markdown
### ADR-{NNN}: {Short imperative title}

- **Date:** YYYY-MM-DD
- **Task:** TASK-XX
- **Status:** ACCEPTED | SUPERSEDED-BY-ADR-XXX
- **Context:** {What situation forced this decision?}
- **Options considered:**
  1. {Option A}
  2. {Option B}
- **Decision:** {What was chosen}
- **Rationale:** {Why}
- **Consequences:** {What this makes easier/harder going forward}
```

---

## Pre-Implementation Decisions (from design phase)

These decisions were made during planning and are captured here for completeness.

### ADR-001: Use synthetic tasks with known ground-truth algorithms

- **Date:** 2025-03-25
- **Task:** Design phase
- **Status:** ACCEPTED
- **Context:** Need to evaluate whether models learn the *algorithm*, not just correlations. Requires knowing the true answer for any input.
- **Decision:** All tasks are generated by reference implementations (Python functions). Every label is verified against the reference at generation time.
- **Rationale:** This is the only way to guarantee label correctness and support out-of-distribution testing against a ground truth.
- **Consequences:** All tasks must have a computable reference algorithm. Tasks where the "algorithm" is unknown or approximate cannot be used.

---

### ADR-002: Two parallel task tracks (Sequence and Classification)

- **Date:** 2025-03-25
- **Task:** Design phase
- **Status:** ACCEPTED
- **Context:** Sequence-only tasks miss an entire family of algorithmically solvable problems that look like tabular ML (rule-based classifiers with mixed inputs).
- **Decision:** Maintain two tracks: Sequence (S-tiers, variable-length token sequences) and Classification (C-tiers, fixed-width mixed numerical/categorical tabular inputs).
- **Rationale:** Broadens the scope of the framework without conflating fundamentally different input representations.
- **Consequences:** Model Harness (SR-5) must handle two distinct encoding strategies. DSLs are separate per track (SR-9, SR-10).

---

### ADR-003: Validation-first development order

- **Date:** 2025-03-25
- **Task:** Design phase
- **Status:** ACCEPTED
- **Context:** ML pipelines can silently produce wrong results (label leakage, non-determinism, off-by-one in splits) that only become visible much later.
- **Decision:** Each shared resource (SR) has a corresponding validation procedure (V) that must pass immediately after the resource is built, before proceeding to the next task.
- **Rationale:** Catching bugs early is far cheaper than discovering them after running 20 experiments.
- **Consequences:** Slightly slower initial development, but much higher confidence in experiment results.

---

### ADR-004: Labels are never noisy — noise is input-only

- **Date:** 2025-03-25
- **Task:** Design phase
- **Status:** ACCEPTED
- **Context:** Need to test model robustness to input perturbation without corrupting the ground truth.
- **Decision:** Noise is added to input features only, after label assignment. Labels always reflect the clean input. Noisy inputs therefore have "stale" labels — intentional.
- **Rationale:** Preserves label integrity. Ensures the reference algorithm remains verifiable. Makes noise robustness a well-posed test.
- **Consequences:** Noise injection happens as a post-processing step in the Data Generator (SR-3), not inside the reference algorithm.

---

### ADR-005: Solvability verdict uses a five-label scale

- **Date:** 2025-03-25
- **Task:** Design phase
- **Status:** ACCEPTED
- **Context:** A binary "solvable/not solvable" verdict loses too much nuance.
- **Decision:** Use STRONG / MODERATE / WEAK / NEGATIVE / INCONCLUSIVE labels based on criteria in `EXPERIMENT_DESIGN.md` Section 9.4.
- **Rationale:** Captures partial success, calibrates expectations, and surfaces inconclusive cases that need more investigation.
- **Consequences:** Report Generator (SR-8) must implement verdict logic exactly per Section 9.4. EXP-D5 validates calibration.

---

## Implementation-Phase Decisions

### ADR-006: Use frozen dataclasses with tuples for schema immutability

- **Date:** 2025-03-25
- **Task:** TASK-01
- **Status:** ACCEPTED
- **Context:** Schema objects will be passed around and stored in registries. Mutable schemas could lead to subtle bugs if modified after registration.
- **Options considered:**
  1. Regular dataclasses with mutable lists
  2. Frozen dataclasses with tuples
  3. Pydantic models with `frozen=True`
- **Decision:** Frozen dataclasses with tuples for collection fields.
- **Rationale:** Immutability prevents accidental mutation. Tuples are hashable (required by frozen dataclass). Standard library dataclasses keep dependencies minimal — Pydantic is available but not needed at this layer.
- **Consequences:** All callers must pass tuples, not lists, when constructing specs. E.g. `values=("a", "b")` not `values=["a", "b"]`.

---

### ADR-007: Use np.random.Generator (modern API) for all sampling

- **Date:** 2025-03-25
- **Task:** TASK-01
- **Status:** ACCEPTED
- **Context:** Need reproducible sampling across the entire pipeline. Two NumPy random APIs exist: legacy `RandomState` and modern `Generator`.
- **Options considered:**
  1. Legacy `np.random.RandomState(seed)`
  2. Modern `np.random.default_rng(seed)`
- **Decision:** Use `np.random.default_rng(seed)` everywhere.
- **Rationale:** Modern API has better statistical properties, is the recommended approach in NumPy docs, and supports `integers()` instead of deprecated `randint()`. Full reproducibility: same seed → same output.
- **Consequences:** All downstream modules (Data Generator, Split Generator, etc.) must also use `default_rng`. Do NOT mix legacy and modern APIs.

---

### ADR-008: Project infrastructure — package layout and dependencies

- **Date:** 2025-03-25
- **Task:** TASK-01 (infrastructure setup)
- **Status:** ACCEPTED
- **Context:** Need a clean Python project layout that supports imports from `src/` subpackages, testing with pytest, and eventual experiment execution.
- **Options considered:**
  1. Flat layout (all .py files in root)
  2. `src/` package with sub-packages for `dsl/` and `models/`
- **Decision:** `src/` package with `__init__.py` files, `tests/` package, `conftest.py` at root for import resolution.
- **Rationale:** Matches the planned file structure in PROJECT_STATUS.md. Sub-packages keep DSL and model code isolated.
- **Consequences:** Imports use `from src.schemas import ...`. The `conftest.py` adds the project root to `sys.path`.
